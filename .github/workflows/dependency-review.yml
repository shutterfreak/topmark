# topmark:header:start
#
#   project      : TopMark
#   file         : dependency-review.yml
#   file_relpath : .github/workflows/dependency-review.yml
#   license      : MIT
#   copyright    : (c) 2025 Olivier Biot
#
# topmark:header:end

name: Dependency Review

on:
  pull_request:
    paths:
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/dependency-review.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Review dependency changes
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Review dependency changes
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high
          fail-on-scopes: runtime
          license-check: true
          vulnerability-check: true
          allow-licenses: >-
            0BSD,
            Apache-2.0,
            BSD-2-Clause,
            BSD-3-Clause,
            HPND-Markus-Kuhn,
            ISC,
            MIT,
            MIT-0,
            MPL-2.0,
            PSF-2.0,
            Python-2.0
          # The 3.4.9 wheel declares MIT and ships only an MIT license, but GitHub's
          # dependency metadata reports an unrelated compound license expression.
          allow-dependencies-licenses: >-
            pkg:pypi/charset-normalizer@3.4.9
